<UserControl x:Class="DaesungEntCleanOven4.View2"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"        
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:appv="clr-namespace:DaesungEntCleanOven4.View"
             d:DesignWidth="1280" d:DesignHeight="834" mc:Ignorable="d"
             FontFamily="Century Gothic" Background="White" SnapsToDevicePixels="True">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <appv:ChannelSummaryPanel Grid.Row="0" Grid.Column="0" Margin="0,0,5,2" DataContext="{Binding Channels[0]}"/>
        <appv:ChannelSummaryPanel Grid.Row="0" Grid.Column="1" Margin="5,0,0,2" DataContext="{Binding Channels[2]}"/>
        <appv:ChannelSummaryPanel Grid.Row="1" Grid.Column="0" Margin="0,2,5,0" DataContext="{Binding Channels[1]}"/>
        <appv:ChannelSummaryPanel Grid.Row="1" Grid.Column="1" Margin="5,2,0,2" DataContext="{Binding Channels[3]}"/>
    </Grid>
</UserControl>
